NESSIE_API_KEY=tu_api_key_aqui
NESSIE_BASE_URL=http://api.reimaginebanking.com
BANXICO_TOKEN=tu_token_sie_banxico_aqui
JWT_SECRET_KEY=genera_una_con_python_-c_"import secrets; print(secrets.token_hex(32))"

# Nota: docker-compose usa este .env (raíz del repo) para el servicio
# backend vía `env_file: .env`. MONGODB_URI no hace falta aquí -- compose
# la sobreescribe para apuntar a su propio contenedor de Mongo interno.
